# SocLite.ai — environment template
#
# The waitlist works out of the box with a local JSON store (no config needed).
# To move to Supabase later, set these and swap the storage adapter in
# app/api/waitlist/route.ts (see the comment block at the top of that file).

# NEXT_PUBLIC_SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
